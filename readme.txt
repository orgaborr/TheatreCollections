Theatre seat reservation program for practicing the Collections Framework.

- master branch uses a usual loop search
- mod branch is set up for binary search
- list-methods branch implements a bubble search method, as well as shuffle/reverse example
- comparator branch utilizes anynimous class Comparator

- map branch uses hash map, not a real Collection, applied in separate MapProgram class